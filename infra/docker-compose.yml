version: '3.8'

services:
  redis:
    image: redis:7
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

# Note: This project uses SQLite for database (configured in Prisma)
# If you want to switch to PostgreSQL:
# 1. Uncomment the db service below
# 2. Update .env DATABASE_URL to: postgresql://user:password@db:5432/to2do
# 3. Update Prisma datasource in backend/prisma/schema.prisma

#  db:
#    image: postgres:15
#    environment:
#      POSTGRES_DB: to2do
#      POSTGRES_USER: user
#      POSTGRES_PASSWORD: password
#    ports:
#      - "5432:5432"
#    volumes:
#      - postgres_data:/var/lib/postgresql/data

volumes:
  redis_data:
# postgres_data: